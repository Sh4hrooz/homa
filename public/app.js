const chat=document.querySelector('#chat'),input=document.querySelector('#input'),send=document.querySelector('#send'),status=document.querySelector('#status'),login=document.querySelector('#login'); let csrf=''; function add(role,text,meta=''){const d=document.createElement('div');d.className='m '+role;d.textContent=text;if(meta){const s=document.createElement('div');s.className='meta';s.textContent=meta;d.appendChild(s)}chat.appendChild(d);chat.scrollTop=chat.scrollHeight;return d} async function api(url,opt){const r=await fetch(url,opt);let j={};try{j=await r.json()}catch{}if(!r.ok)throw new Error(j.error||'خطای ارتباط');return j} async function load(){try{const me=await api('/api/me');if(!me.authenticated){login.style.display='grid';return}csrf=me.csrf||'';const a=await api('/api/history');a.forEach(x=>add(x.role==='user'?'user':'assistant',x.content));const h=await api('/api/health');status.textContent=h.providers.length?متصل · ${h.providers.length} مدل:'آماده · بدون مدل خارجی';input.focus()}catch(e){status.textContent='هسته آماده نیست'}} document.querySelector('#loginBtn').onclick=async()=>{const b=document.querySelector('#loginBtn'),err=document.querySelector('#loginErr');b.disabled=true;err.textContent='';try{const loginResult=await api('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:document.querySelector('#password').value})});csrf=loginResult.csrf||'';login.style.display='none';load()}catch(e){err.textContent=e.message}finally{b.disabled=false}}; document.querySelector('#password').onkeydown=e=>{if(e.key==='Enter')document.querySelector('#loginBtn').click()}; document.querySelector('#logout').onclick=async()=>{await api('/api/logout',{method:'POST',headers:{'X-HOMA-CSRF':csrf}});location.reload()}; form.onsubmit=async e=>{e.preventDefault();const text=input.value.trim();if(!text||send.disabled)return;add('user',text);input.value='';send.disabled=true;status.textContent='هما داره فکر می‌کنه…';const typing=add('assistant','در حال فکر کردن…');typing.classList.add('typing');try{const j=await api('/api/chat',{method:'POST',headers:{'Content-Type':'application/json','X-HOMA-CSRF':csrf},body:JSON.stringify({message:text})});typing.remove();add('assistant',j.reply,j.agents?.length>1?جمع‌بندی ${j.agents.length} عامل: '');status.textContent=j.connected?متصل · ${j.agents?.length||1} مدل:'بدون مدل خارجی'}catch(e){typing.remove();add('assistant','خطا: '+e.message);status.textContent='خطا'}finally{send.disabled=false;input.focus()}}; input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});document.querySelector('#clear').onclick=async()=>{if(confirm('گفتگو پاک شود؟')){await api('/api/clear',{method:'POST',headers:{'X-HOMA-CSRF':csrf}});chat.innerHTML=''}};load();
if ('serviceWorker' in navigator) window.addEventListener('load',()=>window.addEventListener('load',()=>navigator.serviceWorker.register('/sw.js').catch(()=>{})));
